{
  "name": "demoqa-playwright",
  "version": "1.0.0",
  "description": "Playwright + TypeScript test framework for demoqa.com",
  "scripts": {
    "test": "npx playwright test",
    "test:elements": "npx playwright test tests/elements",
    "test:forms": "npx playwright test tests/forms",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^5.3.0"
  }
}
